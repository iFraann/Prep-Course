homework1.txt

//giVariable

Una variable es el lugar donde guardamos datos, bajo determinado nombre. Y con ese nombre tendremos acceso al dato ya almacenado.

var soyUnaVariable = "Con este dato"

//Strings

Los strings son un tipo de dato, conformado por una cadena de caracteres, y se reconocen a través las "".

"Hola soy un string"

//Funciones 

Una función es un bloque de código, que realiza determinada acción.
La definimos con la palabra function
Tendrá un nombre, un argumento (definido entreparentesis) y el código a ejecutar {definido entre llaves}.
El return, es una declaración de retorno. Es por donde va a escapar el dato de la función, y la detendrá para devolver lo que especifiquemos.

// if

El ciclo if es una declaración lógica.
Si determinada expresion es true, ejecuta un código, si es false, ejecuta otro 

//Boolean

Los boolean son otro tipo de datos primitivos, que tiene dos posibles valores :
true
false